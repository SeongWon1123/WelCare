// This file defines the color palette used throughout the app.  It mirrors the
// definitions from Color.js but is placed in a `constants` directory so that
// screens can import it consistently as `./constants/Colors`.
export default {
  background: '#F5F3EF',      // 베이지 배경
  darkGreen: '#2C3E2E',       // 다크 그린
  lightGreen: '#8B9D83',      // 라이트 그린
  gray: '#6B7565',            // 회색
  white: '#FFFFFF',           // 흰색
  inputBg: '#E8E6E0',         // 인풋 배경
  red: '#D32F2F',             // 빨강 (녹음 중)
};